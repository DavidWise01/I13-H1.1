# T5 — one 40D tensor closes as one universe

Canonical structure:

```text
YOU_0D
  -> UNIVERSE[U000]
  -> {
       D00-D09 : DIRECT
       D10-D19 : INVERSE
       D20-D29 : MIRROR
       D30-D39 : INVERSE-MIRROR
     }
  -> AMPLITUDE {0,+2}
  -> BOUNDARY {IN,OUT}
```

The four distinctions are four banks over one ten-dimensional nested body.
They produce exactly 40 dimensions. Amplitude, boundary, observer, and universe
index control or bind the tensor; they do not add geometric dimensions.

The 40 dimensional addresses combine with two amplitudes and two boundary
orientations to produce 160 controlled states. Every state is bound to U000
and receives a unique deterministic receipt.

This test supersedes the semantic interpretation in T4 that raised one nested
address capacity to the tenth power. T4 remains preserved as provenance; T5 is
the corrected dimensional model.
