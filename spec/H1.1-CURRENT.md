# H1.1 current semantic edge

```text
OLOGY surface: u32 = [x:16 | y:16]
point        : ln = <x,y>
voxel root   : V(ln)
local depth  : z
```

Current transition form:

```text
[c[v[
    (),
    {},
    ()
]]cv]
```

Meaning:

```text
c[v[  Cortex enters voxel at current surface root
()    state in
{}    local working volume/context
()    state out
]]    leave/close local voxel
cv    Cortex Verifier gates exit
```

Surface and local motion remain orthogonal:

```text
MOVE   = delta x/y
BURROW = delta z at same x/y root
```

The Queen is the mobile Cortex role on the OLOGY surface. A useful direction is not sufficient authority; CV must pass before a surface transition is enabled.
