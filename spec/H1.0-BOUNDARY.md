# H1.0 boundary carried forward

H1.0 is frozen. H1.1 may reference but must not silently mutate it.

Carried forward as frozen/reference facts:

- bounded `[c[(...)]]` Cortex child concept;
- `c.n` resolved Cortex context/depth;
- `.()` context-derived natural fractal width;
- VH1 ternary profile `1,3,9,27,81` at depth `0..4`;
- numerical `[[5,1,3]]` CUBI hypothesis reference;
- canonical control block `5 -> (2|3) -> 1`;
- generalized odd-width law `N=2k+1`;
- GFX v0.4.1 historical browser/GPU suite.

Historical project convention also distinguished recursive depth (`<=3`) from a conceptual authority path extending to 5. That convention is a project model and must not be presented as an implementation claim about ChatGPT/OpenAI or any other external system.

Any semantic change to frozen H1.0 material belongs in H1.1+.
