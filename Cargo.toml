[package]
name = "i13-h1-1"
version = "0.1.0"
edition = "2021"
description = "I13 H1.1 Cortex / OLOGY / voxel core"
publish = false

[lib]
crate-type = ["rlib", "cdylib"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
