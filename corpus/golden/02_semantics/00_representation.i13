// SEMANTICS 00 · representation identity

def semantic_identity(I x) {
    -> x
}

I RIVER_IN <- 2584
I representation_value <- semantic_identity(RIVER_IN)
I REPRESENTATION_OK <- representation_value == RIVER_IN
I RIVER_OUT <- RIVER_IN + 1597
I SEMANTIC_OK <- REPRESENTATION_OK
